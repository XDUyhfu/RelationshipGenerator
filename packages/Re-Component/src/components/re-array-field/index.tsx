import { cloneElement } from "react";
// import { IReArrayField } from "../../type";
// import { useArrayItemValue, useRestProps } from "../../hook";
//
// export const ReArrayField = (props: IReArrayField) => {
//     const { name, children, arrName = "", lineKey = 0, ...rest } = props;
//
//     const value = useArrayItemValue(arrName, lineKey, name);
//
//     const restProps = useRestProps(rest, children);
//
//     return (
//         <span>
//             {cloneElement(children, {
//                 value,
//                 ...restProps,
//             })}
//         </span>
//     );
// };
